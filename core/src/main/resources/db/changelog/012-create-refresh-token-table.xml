<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
    http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.8.xsd">

    <changeSet id="create-refresh-tokens-table" author="Artash Shaghoyan">
        <preConditions onFail="MARK_RAN">
            <not>
                <tableExists tableName="refresh_tokens" schemaName="agro_trade"/>
            </not>
        </preConditions>

        <createTable tableName="refresh_tokens">
            <column name="id" type="bigint" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>

            <column name="token" type="varchar(512)">
                <constraints nullable="false" unique="true"/>
            </column>

            <column name="user_id" type="bigint">
                <constraints nullable="false"/>
            </column>

            <column name="expiry_date" type="timestamp">
                <constraints nullable="false"/>
            </column>

            <column name="revoked" type="boolean" defaultValueBoolean="false">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <addForeignKeyConstraint
                baseTableName="refresh_tokens"
                baseColumnNames="user_id"
                referencedTableName="user"
                referencedColumnNames="id"
                constraintName="fk_refresh_tokens_user"
                onDelete="CASCADE"/>

    </changeSet>

    <changeSet id="refresh-tokens-roles-table-rollback" author="Artash Shaghoyan">
        <rollback>
            <dropTable
                    tableName="refresh_tokens"
                    schemaName="agro_trade"
                    cascadeConstraints="true"/>
        </rollback>
    </changeSet>

</databaseChangeLog>