<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
    http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.8.xsd">

    <changeSet id="create-user-roles-table" author="Artash Shaghoyan">
        <preConditions onFail="MARK_RAN">
            <not>
                <tableExists tableName="user_roles" schemaName="agro_trade"/>
            </not>
        </preConditions>

        <createTable tableName="user_roles" schemaName="agro_trade">
            <column name="user_id" type="bigint">
                <constraints nullable="false"/>
            </column>
            <column name="role_name" type="varchar(50)">
                <constraints nullable="false"/>
            </column>
        </createTable>
        <addForeignKeyConstraint
                baseTableName="user_roles"
                baseColumnNames="user_id"
                referencedTableName="user"
                referencedColumnNames="id"
                constraintName="fk_user_roles_user"
                onDelete="CASCADE"/>

    </changeSet>

    <changeSet id="user-roles-table-rollback" author="Artash Shaghoyan">
        <rollback>
            <dropTable
                    tableName="user_roles"
                    schemaName="agro_trade"
                    cascadeConstraints="true"/>
        </rollback>
    </changeSet>

</databaseChangeLog>